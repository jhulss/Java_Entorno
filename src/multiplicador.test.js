import multi from "./multiplicador";

describe("Multiplocar", () => {
    it("deberia multiplicar dos numeros", () => {
      //expect(multi(3, 2)).toEqual(6);
      expect(multi(3,1)).expect(6);
    });
  });